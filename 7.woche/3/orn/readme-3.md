```javascript


```

```markdown
# `forEach` Kullanarak Ürün Listesi ile Çalışma Soruları

Aşağıdaki soruları, `forEach` metodunu kullanarak çözmeniz gerekmektedir. Her soru için `categoryData` yapısını kullanarak çıktılar oluşturun.

---
const categoryData

### Soru 1
`forEach` kullanarak tüm `items` içindeki ürünlerin fiyat bilgilerini (`prices`) konsola yazdırın. Ürün adı ve fiyat bilgisi `"Item Name: Price"` formatında görünsün.

---

### Soru 2
`forEach` kullanarak her bir `subcategories` içindeki tüm ürünleri ve açıklamalarını (`description`) listeleyin. Çıktı olarak ürün adı ve açıklaması `"Item Name: Description"` formatında olsun.

---

### Soru 3
`forEach` kullanarak, her bir `subcategory` içindeki `items` listesinde `allergene` listesinde `"a"` alerjenine sahip olan ürünlerin isimlerini ve açıklamalarını konsola yazdırın.

---

### Soru 4
`forEach` ile `items` içinde fiyatı `5.0`'dan büyük olan ürünleri bulun ve bu ürünlerin adlarını ve fiyatlarını konsola yazdırın.

---

### Soru 5
Her bir `subcategory` içinde `extras` özelliği olan ürünleri `forEach` ile bularak konsola yazdırın. Ürün adı ve ekstra malzemeler şeklinde bir çıktı oluşturun.

---

### Soru 6
`forEach` kullanarak her bir `subcategory` içindeki ürünlerin `nr` değerini ve `type` özelliklerini konsola yazdırın.

---

### Soru 7
`forEach` ile her `subcategory` içindeki `items` listesindeki ürünlerden "Spinat" ekstra malzemesine sahip olan ürünlerin adını ve fiyatını konsola yazdırın.

---

### Soru 8
`forEach` kullanarak `subcategories` içindeki ürünlerden "Pizzabrötchen" ismine sahip olanların `prices` bilgilerini `"Item Price"` formatında konsola yazdırın.

---

### Soru 9
`forEach` ile tüm `items` listesinde bulunan her ürünün `allergene` listesindeki tüm alerjenleri ve ürün adını `"Item Name: Allergen"` formatında konsola yazdırın.

---

### Soru 10
`forEach` kullanarak `subcategories` içindeki her bir üründe `extras` kısmındaki tüm ekstra malzemelerin listesini konsola yazdırın. Her bir ürün için `"Item Name: Extra Items"` formatında gösterilsin.

--- 
``` 

Bu formatta, her soru ürünlerin özelliklerine odaklanarak `forEach` kullanımıyla detaylı bir analiz sağlar. Her bir soru, kullanıcıların `categoryData` içindeki veriyi anlaması ve işlemesi için yapılandırılmıştır.